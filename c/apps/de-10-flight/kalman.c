#include "kalman.h"

// uint16_t simple_filter(uint16_t val)
// {
//     // float a = 0.7;
//     // // Predict
//     // float xhat = a * xhat;
//     // float p = a * p * a;

//     // // Update
//     // g = p / (p + r);
//     // xhat = xhat + g * (z - xhat);
//     // p = (1 - g) * p;
// }